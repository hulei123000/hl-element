import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'
import { withInstall } from '@hl-element/utils'

export const HlButton = withInstall(Button)
export const HlButtonGroup = withInstall(ButtonGroup)
export * from './type'